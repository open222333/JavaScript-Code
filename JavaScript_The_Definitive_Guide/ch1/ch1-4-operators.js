//運算子作用於值
3 + 2
3 - 2
3 * 2
3 / 2
points[1].x - points[0].x // 1
'3' + '2' // "32"

let count = 0
count++; // 遞增
count--; // 遞減
count += 2; // count = count + 2
count *= 3; // count = count * 3
console.log(count)

let x = 2,
  y = 3 // 定義變數
x === y // false 是否相等
x !== y // true 不相等
x <= y
x > y
x >= y
(x === 2) && (y === 3) //  && AND
(x > 3) || (y < 3) // || OR
!(x === y) // 反轉
