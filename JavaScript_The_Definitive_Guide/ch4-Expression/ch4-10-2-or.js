// OR || 邏輯運算子
// let max = maxWidth || preferences.maxWidth || 500
// console.log(max)

// 把o的特性拷貝到p 回傳p ES6之前
function copy(o, p) {
    p = p || {} // 若沒有為p傳入物件 則使用一個新創物件
        // 函式主體
}

// 把o的特性拷貝到p 回傳p ES6之後
function copy(o, p = {}) {
    // 函式主體
}